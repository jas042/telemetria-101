# Exercício Módulo 2

## Objetivo

Subir dois Webservers diferentes usando Docker Compose:
- **NGINX** na porta `8080`
- **Apache HTTPD** na porta `8081`

---

## Passos para executar

### 1) Subir dois Webservers (NGINX e Apache)

Crie um arquivo `docker-compose.yml` com o seguinte conteúdo:

```yaml
version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    container_name: nginx_server

  apache:
    image: httpd:latest
    ports:
      - "8081:80"
    container_name: apache_server



# Exercício Módulo 2

1) Subir dois Webserver diferentes (NGINX porta 8080 + Apache porta 8081) usando docker-compose
2) Testar os acesso com o navegador para a porta 8080 e 8081
3) Verificar os Logs de ambos os containers
4) Remover os containers
